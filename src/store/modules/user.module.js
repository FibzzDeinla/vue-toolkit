
// State
const state = {
    roles: []
}; 

// Actions
const actions = {
    getUserInfo: async({ commit }) => {
    }
  };

// Mutations
const mutations = {};

export const user = {
    namespaced: true,
    state,
    actions,
    mutations
  };